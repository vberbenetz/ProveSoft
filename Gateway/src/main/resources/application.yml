logging:
  level:
    org.springframework: INFO
security:
  sessions: ALWAYS
zuul:
  routes:
    ui:
      url: http://localhost:8081
    resource:
      url: http://localhost:9000
multipart:
  max-file-size: 100MB
  max-request-size: 100MB
      